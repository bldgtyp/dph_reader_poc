# 00_Context — the foundation layer

Everything durable we know about the systems DesignPH-PLUS sits between: designPH, SketchUp, Pyodide,
and the honeybee stack. Produced across spike Phases 0–3 (2026-08-19), substantially extended by the
POC's **first real end-to-end export**, the **full five-model corpus sweep**, and POC-5's
**downstream-consumer checks** (ph-navigator, live — `HONEYBEE_STACK.md` §6.4) — all 2026-08-21,
which is where most of the sharp-edged detail now comes from. **The POC is closed; this layer is
the part of its research a future V-0 leans on hardest.** Written so later work does not rediscover
any of it.

**Evidence base as of 2026-08-21:** five real project models spanning designPH 2.1.15–2.2.29 and
SketchUp 22–26, captured live and reconciled against an offline scan of all 14 corpus files. 545
classified faces, 239 windows, 99 thermal bridges, 3 TFA zones, four assembly tiers, and designPH's
own U-/R-value calculator as ground truth for the arithmetic.

**If you read one file, read [`CONSTRAINTS.md`](CONSTRAINTS.md)** — start at its §8.1, "what the
first real export settled, and what it opened".

> **Reading a rule dated 2026-08-21?** It came from running the whole pipeline against real corpus
> models in SketchUp 2022, with the output checked in Rhino/Grasshopper and the arithmetic checked
> against designPH's own dialog and the PHPP. Those are *measurements*, not inferences — and a
> striking number of them **overturned a rule that had been inferred from a single sample**: the
> window unit table, the aperture rectangle, the `*ID`/`*Auto` exclusivity claim, the section
> percentages, and both live-vs-offline count invariants. Where a claim is still inferred, it says so.
>
> ⚠ **The pattern is consistent enough to plan around: n=1 evidence on this project has been wrong
> more often than it has been right.** Adelphi alone masked every one of the count-invariant bugs;
> one window "confirmed" a unit table that holds on 20 of 46; three percentages fitted a scale rule
> that was backwards. Before generalising, check the claim against the whole corpus — the baseline
> and all five captures exist precisely so that is cheap.

| File | What is in it |
|---|---|
| ★ [`CONSTRAINTS.md`](CONSTRAINTS.md) | **Start here.** Every hard limit, blocker and non-negotiable rule, graded 🔴/🟠/🔵, plus §8.1's summary of the first real export, the unsolved design problems, and what is still untested |
| [`DESIGNPH.md`](DESIGNPH.md) | Basics — what designPH is, authors, version history, licensing, install locations, the beta/sub-version mechanism |
| [`DESIGNPH_DATA_MODEL.md`](DESIGNPH_DATA_MODEL.md) | **The designPH record.** `DesignPH_dict`, model- vs face-level keys, PHPP area groups, the `*ID`/`*Auto` rule, the Marshal blob convention, edge cases |
| [`DESIGNPH_FILE_FORMATS.md`](DESIGNPH_FILE_FORMATS.md) | On-disk formats: shipped CSV libraries, component/climate ID conventions, `.skm` materials, the `.skp` binary layout and its parsing traps |
| [`PPP_EXPORT.md`](PPP_EXPORT.md) | The `.ppp`: the licence line we drew, why, and the minimum observed — deliberately not a format description. **The format itself is now cataloged in `phi-rules`** (see below) |
| [`SKETCHUP_RUNTIME.md`](SKETCHUP_RUNTIME.md) | **SketchUp as a host.** Ruby 2.7, extension packaging, `HtmlDialog`'s frozen Chromium, the `file://` dead end, the threading rules that hang it, the bridge and its measured capacity, model-reading traps |
| [`SDK_RUNTIME.md`](SDK_RUNTIME.md) | **SketchUp WITHOUT SketchUp** — the C SDK route. ⛔ Its availability block (Request Access form, no public download), the public doxygen surface, the glue query, both id flavours, and what documentation cannot answer |
| [`PYODIDE_RUNTIME.md`](PYODIDE_RUNTIME.md) | **The WASM Python runtime.** The version ceiling and why, what `pyodide-core` contains, installing by unpack, boot sequence, measured performance and memory |
| [`HONEYBEE_STACK.md`](HONEYBEE_STACK.md) | **honeybee / honeybee-ph / PHX.** The eight wheels, declared-vs-imported dependencies, import order, API traps, the write/read cost asymmetry, schema validation, PHX's reachable write path, licences |
| [`DATA_CONTRACTS.md`](DATA_CONTRACTS.md) | **The translation spine.** The shape data takes at every hop — designPH face → JSON → bridge → honeybee → HBJSON — and the rules governing each conversion |
| [`tools/skp_attr_dump.py`](tools/skp_attr_dump.py) | Standalone reader that dumps any `.skp`'s attribute dictionaries without SketchUp. `uv run` — no venv |
| [`tools/skp_decode_tables.py`](tools/skp_decode_tables.py) | Decodes the model-level **Marshal tables** out of a `.skp` — `:TOKENS` header plus sample rows. How the `frames_ud`/`glazing_ud` schemas were finally read. ⚠ `tracker_data` decodes too, and it holds usernames |

## ⚠ Some of this now lives in `phi-rules` as well

The durable designPH knowledge in `DESIGNPH*.md` and the `.ppp` format have been consolidated into
the BLDGTYP **`phi-rules`** corpus as a reference ruleset, `rulesets/designph-2x-r1/` — reachable
from any repo through the **`phi-rules` skill**. That is where a *tool* looks it up; these files stay
the DesignPH-PLUS investigation record.

| There | Here |
|---|---|
| `tool/D01`–`D08` — designPH as a data store, generalised | `DESIGNPH.md`, `DESIGNPH_DATA_MODEL.md`, `DESIGNPH_FILE_FORMATS.md` — the same facts plus this project's reasoning and dead ends |
| `ppp/D09`–`D12` — the `.ppp`, mapped onto PHPP's named ranges | `PPP_EXPORT.md` — the licence line, which is a **DesignPH-PLUS** decision |

**Keep them in sync in one direction:** when something durable about designPH is *learned*, it goes
to `phi-rules`; this repo cites it. Two facts already flowed the other way and are recorded in both —
the `tempZone` refinement (`DESIGNPH_DATA_MODEL.md` §5.3.1) and the `.ppp` finding
(`PPP_EXPORT.md` header).

## How these fit together

```
DESIGNPH*.md        what designPH stores          ─┐
SKETCHUP_RUNTIME.md what the host allows          ─┼─► DATA_CONTRACTS.md ─► HBJSON
PYODIDE_RUNTIME.md  what the runtime can execute  ─┤     (the translation spine)
HONEYBEE_STACK.md   what the libraries require    ─┘
                                                        CONSTRAINTS.md summarises all of it
```

## What this grounds

- [`../DESIGNPH-PLUS_PRD.md`](../DESIGNPH-PLUS_PRD.md) — v1 requirements. §7.1's runtime decision,
  §7.4's version floor and §8's translation rules all rest on these notes.
- [`../planning/`](../planning/00_OVERVIEW.md) — the phased spike plan and its results.

## Related, outside this folder

- **BT Attribute Inspector** — read-only SketchUp extension.
  `~/Library/Application Support/SketchUp 2022/SketchUp/Plugins/bt_inspector/`. Use it for *current*
  model state; the offline parser sees historical state too and cannot tell the difference.
- **Phase 3 spike code** — [`../planning/spikes/pyodide/`](../planning/spikes/pyodide/.index.md).
  The working extension, the Chrome/Chromium-88 harness, and the vendoring and build scripts.
- **Corpus** — `../_adephi_st_example_files/` (one building, six formats) and 11 real project models
  under `~/Dropbox/bldgtyp/*/08_DesignPH/`, all baselined in
  `../planning/RESULTS/PHASE-0_corpus-baseline.md`.

## Status — 2026-08-19

✅ **Phases 0–3 complete.** The `*ID`/`*Auto` question is **settled** (`DESIGNPH_DATA_MODEL.md` §6.5
— a coalesce, version-independent). The runtime is **decided**: Pyodide 0.24.1 in `HtmlDialog`,
served over loopback, proven end to end in SketchUp against a real designPH model.

🔴 **Blocking v1:** the AGPL licence question (`../planning/RESULTS/PHASE-3_licence-question.md`).

Remaining designPH gaps are listed in `DESIGNPH_DATA_MODEL.md` §12; the open *design* problems are in
[`CONSTRAINTS.md`](CONSTRAINTS.md) §7.
