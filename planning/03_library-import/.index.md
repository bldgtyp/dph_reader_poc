# planning/03_library-import — POC #3: writing assemblies and window types INTO designPH

**⭐ STATUS: Spike L-A PASS (2026-08-31, agent prep + all Ed sessions in one evening).**
designPH accepts foreign model-level library writes end to end; all nine open questions answered;
durable facts folded into `00_Context/DESIGNPH_DATA_MODEL.md` §14. **▶ L-B is next.**

POCs #1 and #2 proved the read direction. This POC asks the write direction, narrowly: **can we
SET designPH library data at the model level** — assemblies, window frames, glazings, written into
the model's `DesignPH_dict` from outside — **such that designPH lists it, computes with it, and
carries it to PHPP?** If yes, the product shape is *PH-Navigator builds the library, the model
imports it*, replacing designPH's laborious assembly builder and window editor for BLDGTYP work.

**Read [`00_LIBRARY-IMPORT_OVERVIEW.md`](00_LIBRARY-IMPORT_OVERVIEW.md)** — the verified facts
(what the record already proves about where and how this data is stored), the nine open questions
O-1…O-9 (acceptance, write timing / hot-swap, rename integrity, entity-dictionary coupling), the
three spikes, the scoped amendment to hard rule 2, and the licensing tasks.

**The primary deliverable is what we learn, written down** (Ed, 2026-08-31): spike code is
throwaway and its quality is irrelevant, but every observation is recorded in `RESULTS/`, durable
facts flow to `00_Context/` and `phi-rules`, and any doc a finding contradicts gets corrected and
marked superseded in the same pass.

| Spike | Gate decides | State |
|---|---|---|
| L-A — the handshake (hand-crafted write → Ed's designPH sessions → capture diff) | **Whether designPH accepts foreign model-level writes at all** — the existential question | ⭐ **PASS** — [`RESULTS/LIBRARY-A_results.md`](RESULTS/LIBRARY-A_results.md) (O-1…O-9 all answered; protocol: [`RESULTS/LIBRARY-A_ed-runbook.md`](RESULTS/LIBRARY-A_ed-runbook.md)) |
| L-B — the PHN → designPH mapping contract | Whether real PHN assemblies/window types reproduce their intended U-values in designPH's own calculator | ▶ **unblocked** — see the overview's revised spike section for what L-A hands it |
| L-C — transport & product shape (sketch only) | The recommended v1 shape | ⛔ after L-B |
| LI-1…LI-3 — licence re-read, PHI-conversation note, internal-only posture | — | ▶ **more urgent after the PASS** — and LI-1 now also records the hard-rule-1 amendment (`../../00_Context/PPP_EXPORT.md` §1) for PHI confirmation |

- `RESULTS/` — the runbook + results skeleton exist; the gate rule from
  [`../.index.md`](../.index.md) applies.
- Spike code: [`../spikes/library-import/`](../spikes/.index.md) — `write_library.rb` (the
  paste-in), `rehearse.py` (offline proof), `dump_model_tables.py` (authoritative before/after
  read), `prep_copies.py`; client-data scratch in its gitignored `_private/` (`MANIFEST.md`).
  Product code, if L-A passes and an importer gets built: `pocs/03_library-import/`.
