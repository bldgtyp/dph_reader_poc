# planning/04_hbjson-to-skp — POC #4: HBJSON → a fresh designPH-ready SketchUp model

**▶ STATUS: SCOPED, NOT STARTED (Ed, 2026-08-31 — product-shape discussion).** Deliberately
deferred behind POC #3's v-0 (Library Sync); Ed: not a day-one feature. The sequencing rule
applies — nothing runs here until the POC #3 L-C gate is evaluated and recorded.

POCs #1/#2 read designPH into HBJSON; POC #3 writes libraries into an existing designPH model.
This POC asks the last leg of the interop loop: **can we generate a NEW `.skp` from scratch out
of an HBJSON — geometry first, optionally classifications and libraries — that designPH adopts
as a legitimate working model?** Fresh scene only, by decision: this is **never** surgery on an
existing designPH model.

**Read [`00_HBJSON-TO-SKP_OVERVIEW.md`](00_HBJSON-TO-SKP_OVERVIEW.md)** — the question, what the
record already constrains, the open questions H-1…H-5, and the existential probe that decides
whether a full plan ever gets written.

| Spike | Gate decides | State |
|---|---|---|
| H-0 — the existential probe (minimal fresh model + one window DC → does designPH adopt it?) | Whether this POC gets a full plan at all | ▶ **scoped, not started** — waits on POC #3's L-C gate |

- Spike code, when it exists: `../spikes/hbjson-to-skp/`; results in `RESULTS/`; durable
  designPH facts to `00_Context/DESIGNPH_DATA_MODEL.md`, per the house rule.
