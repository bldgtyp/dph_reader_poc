# planning/HEADLESS — the headless-reader phase

**The third body of work in this repo, and the only active one (opened 2026-08-28).** The POC
(`../POC/`) closed PASS on 2026-08-21 and proved the pipeline *inside* SketchUp; this phase asks
whether the same read can happen **with no SketchUp at all** — a headless service, driven by a
Dropbox watcher (the pholio model), scraping designPH data passively with no user in the loop and
no SketchUp seat on any server. The proposed route is the **SketchUp C SDK**.

**Read [`00_HEADLESS_OVERVIEW.md`](00_HEADLESS_OVERVIEW.md) first** — the question, why the C SDK,
the rules in force, and the licensing checklist.

| File | Spike | Gate decides |
|---|---|---|
| [`00_HEADLESS_OVERVIEW.md`](00_HEADLESS_OVERVIEW.md) | — | Nothing — context, rules, sequencing, licensing checklist |
| [`HEADLESS-A_sdk-feasibility.md`](HEADLESS-A_sdk-feasibility.md) | A | **Whether the C SDK exposes the data at all** — glue query, typed attributes, edges, Marshal tables, live-vs-historical, transforms |
| [`HEADLESS-B_contract-identity-gate.md`](HEADLESS-B_contract-identity-gate.md) | B | Whether a headless collector is a **drop-in capture device** — contract v2 out, identical to the five live captures, translator to the POC's own numbers |

- [`code-review.md`](code-review.md) — pre-Spike-A review of the three planning docs, **now
  dispositioned and folded in (2026-08-28, see its disposition table)**: 7 of 8 findings adopted
  (fixture staging, NUL-truncation proof, pyslapi demotion, entity-basis counting, G6
  verbatim-record decision, G0 boot pre-gate, H1/H7 hardening); item 2's headline was itself wrong
  (the G1 API name exists — verified, SketchUp 2018 API 6.0) though its `SUFaceGetOpenings`
  cross-check was kept; the ADR-019 softening was rejected (number verified in pholio).
- [`RESULTS/`](RESULTS/) — one `HEADLESS-X_results.md` per spike, negative results included. Written
  *before* the next spike starts (the standing rule, unchanged from the spike phases and the POC).
  ▶ [`HEADLESS-A_results.md`](RESULTS/HEADLESS-A_results.md) — the availability block, the API-surface
  answer, and the counting-basis finding that Spike B's identity gate depends on.
- Spike code: [`../spikes/headless/`](../spikes/) — throwaway, PEP 723 + `uv run`, kept regardless
  of outcome. Client-data scratch (corpus copies, captures): `../spikes/headless/_private/` —
  gitignored, `MANIFEST.md`-tracked, same pattern as `poc/_private/`.

## Status

| Item | State |
|---|---|
| Spike A — C SDK feasibility | ✅ **PASS (2026-08-29)** — [`RESULTS/HEADLESS-A_results.md`](RESULTS/HEADLESS-A_results.md). Headless, no SketchUp: **545/545 faces · 239/239 windows · 99/99 edges · 63/63 tables · 15/15 files**, ≈3-4 s/model. ⚠ Run on a **third-party SDK build** — Trimble's is behind an access form (results §1) |
| Spike B — contract-v2 identity gate | ▶ **UNBLOCKED, not started** — [`HEADLESS-B_contract-identity-gate.md`](HEADLESS-B_contract-identity-gate.md). Its H0 revision pass runs first and must fold in results §9 (key on the path-qualified `id`, never `entity_id`; the get-or-create side effect and the never-save invariant; `SUFaceGetAreaWithTransform`; `SUFaceGetNumOpenings` as a second host test) |
| Spike C — deployment (macOS worker vs Wine/Linux vs on-user-machine) | Sketched only (A doc §8, overview §5) — blocked on Spike B; partly a pholio ADR conversation |
| Licensing checklist L1–L3 (SDK EULA · AGPL §13 reframing · designPH posture) | ⛔ **L1 unstartable** — the EULA ships inside the gated download; ▶ **Ed is filing Trimble's access form**, which is the only route to it. L2 (AGPL §13) is unaffected by the block and is now the more actionable of the two. Overview §6 |

## The rule

**Do not start a spike until the previous spike's gate has been evaluated and recorded in
`RESULTS/`.** Same rule as Phases 0–5 and the POC; it exists to stop early, not to complete
the sequence.

*Provenance: drafted 2026-08-28; briefly scaffolded in `00_PH_Tools/design-ph-plus/` before being
moved here the same day.*
