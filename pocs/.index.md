# pocs — POC code, one folder per POC

Runnable code produced by the POCs planned in [`../planning/`](../planning/.index.md). Numbering
matches the planning folders. **All of it is internal-only and never distributed** — the AGPL
question blocks release, not internal work.

| Folder | POC | State |
|---|---|---|
| [`01_sketchup-export/`](01_sketchup-export/.index.md) | The SketchUp extension + Pyodide translator (POC #1) | ✅ complete; `make ci` is the offline gate |
| `02_headless-reader/` | *(does not exist)* — POC #2 produced spike-grade code only, kept at [`../planning/spikes/headless/`](../planning/spikes/headless/) per the spike-code rule. `collector.py` there is the headless contract-v2 collector | ✅ spikes A+B pass |
| `03_library-import/` | *(not started)* — created if POC #3's Spike L-A passes and an importer gets built | ▶ scoped |
